---
description: React and TypeScript conventions
globs: "**/*.{ts,tsx}"
alwaysApply: false
---

# React + TypeScript

- Functional components and hooks only.
- Explicit props types; avoid `any`.
- Prefer named exports for components.
- Colocate feature types in the feature folder when not shared.
